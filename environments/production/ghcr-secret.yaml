apiVersion: v1
kind: Secret
metadata:
  name: ghcr-secret
  namespace: default
type: kubernetes.io/dockerconfigjson
data:
  # Generate this with:
  # kubectl create secret docker-registry ghcr-secret \
  #   --docker-server=ghcr.io \
  #   --docker-username=janeonthegame \
  #   --docker-password=YOUR_GITHUB_PAT \
  #   --docker-email=your@email.com \
  #   --dry-run=client -o yaml | grep .dockerconfigjson
  
  .dockerconfigjson: eyJhdXRocyI6eyJnaGNyLmlvIjp7InVzZXJuYW1lIjoiamFuZW9udGhlZ2FtZSIsInBhc3N3b3JkIjoiWU9VUl9HSVRIVUJfUEFUIiwiZW1haWwiOiJ5b3VyQGVtYWlsLmNvbSIsImF1dGgiOiJhbUZ1Wlc5dWRHaGxaMkZ0WlRwWlQxVlNYMGRKVkVoVlFsOVFRVlE9In19fQ==

---
# NOTE: The above .dockerconfigjson is a PLACEHOLDER!
# 
# To generate the real secret, run this command and copy the .dockerconfigjson value:
#
# kubectl create secret docker-registry ghcr-secret \
#   --docker-server=ghcr.io \
#   --docker-username=janeonthegame \
#   --docker-password=YOUR_ACTUAL_GITHUB_PERSONAL_ACCESS_TOKEN \
#   --docker-email=your-actual-email@example.com \
#   --namespace=default \
#   --dry-run=client -o yaml
#
# Then replace the .dockerconfigjson value above with the generated one.
